{
  "name": "yumia",
  "version": "0.1.0",
  "private": true,
  "description": "YUMIA — Le Copilote IA Mondial des Expériences du Quotidien",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:shared": "npm run build --workspace @yumia/shared",
    "dev:api": "npm run start:dev --workspace @yumia/api",
    "dev:mobile": "npm run start --workspace @yumia/mobile",
    "generate-assets": "node scripts/generate-assets.mjs",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "//overrides": [
    "react-native-worklets porte un runtime moteur JS + natif qui doivent",
    "correspondre exactement. Le runtime Expo (SDK 54) embarque la 0.5.1 ;",
    "sans cette contrainte, npm installe la 0.8.3 pour satisfaire la plage",
    "large de react-native-reanimated, et on se retrouve avec deux copies —",
    "d'ou un plantage au demarrage de Reanimated."
  ],
  "overrides": {
    "react-native-worklets": "0.5.1"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "expo-av": "~16.0.8",
    "expo-camera": "~17.0.10"
  }
}
